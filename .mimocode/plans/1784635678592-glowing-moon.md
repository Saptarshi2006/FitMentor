# Fix FitMentor Site Flow & Connect to AWS

## Changes Required

### 1. Remove `/login` route
- Delete `apps/web/src/routes/login.tsx`

### 2. Fix `/signup` page
- Add "Already have an account? Sign in" link
- Remove disabled Apple sign-in button
- Remove feature icons section (keep it clean like signin)

### 3. Fix `/signin` page  
- Remove disabled Apple sign-in button (keep only Discord)

### 4. Fix Home tab → `/dashboard`
- `apps/web/src/components/MobileShell.tsx`: Change `{ to: "/", label: "Home" }` to `{ to: "/dashboard", label: "Home" }`

### 5. Fix Profile logout → `/`
- Already correct in `apps/web/src/routes/profile.tsx` line 204

### 6. Fix Dashboard redirect
- `apps/web/src/routes/dashboard.tsx`: Already has `if (!p) { navigate({ to: "/onboarding" }); return; }`

### 7. Fix OAuth callback to check if profile exists
- New users → redirect to `/onboarding`
- Existing users → redirect to `/dashboard`
- This requires checking if the user has a profile in the database after Discord auth

### 8. Cookie/Session storage in Redis
- Update `apps/api/src/routes/auth.rs` to store sessions in Redis (ElastiCache) instead of Cloudflare KV
- The auth callback on EC2 already does this

## Files to Modify

1. `apps/web/src/routes/login.tsx` - DELETE
2. `apps/web/src/routes/signup.tsx` - Add sign in link, remove Apple button
3. `apps/web/src/routes/signin.tsx` - Remove Apple button
4. `apps/web/src/components/MobileShell.tsx` - Fix Home tab
5. `apps/web/src/routes/dashboard.tsx` - Verify onboarding redirect (already done)
6. `apps/web/src/routes/profile.tsx` - Verify logout to `/` (already done)

## Verification

1. Landing page → Click "Get Started" → Goes to `/signin`
2. `/signin` → Click "Sign up" → Goes to `/signup`
3. `/signup` → Click "Sign in" → Goes to `/signin`
4. Sign in with Discord → New user → `/onboarding` → `/dashboard`
5. Sign in with Discord → Existing user → `/dashboard`
6. Home tab → `/dashboard`
7. Profile → Logout → `/` (landing page)
